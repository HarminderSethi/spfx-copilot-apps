/// <reference types="node" />
import fs from 'fs';import path from 'path';
import { INTENTS } from './catalog';

describe('purpose-specific visual coverage',()=>{
  const sharedRoot=path.resolve(__dirname,'..','..','src','shared');
  const inlineSource=fs.readFileSync(path.join(sharedRoot,'ServiceInlineExperiences.tsx'),'utf8');
  const dashboardSource=fs.readFileSync(path.join(sharedRoot,'ServiceDashboards.tsx'),'utf8');
  it('routes every catalog intent through an explicit inline case',()=>{for(const intent of INTENTS)expect(inlineSource.includes(`case "${intent.key}"`)||inlineSource.includes(`case '${intent.key}'`)).toBe(true);});
  it('keeps every inline default layout identity unique',()=>{const staticLayouts=Array.from(inlineSource.matchAll(/data-layout="([^"]+)"/g),match=>match[1]);const dynamicLayouts=['incident-candidate-compact-review-queue','service-recovery-compact-review-queue'];const layouts=[...staticLayouts,...dynamicLayouts];expect(new Set(layouts).size).toBe(layouts.length);expect(layouts.length).toBeGreaterThanOrEqual(23);});
  it('keeps four dashboard identities and compositions distinct',()=>{const layouts=['my-queue-judgment-cockpit','customer-360-promise-constellation','resolution-room-evidence-command','service-operations-intervention-command'];for(const layout of layouts)expect(dashboardSource).toContain(`data-layout="${layout}"`);expect(new Set(layouts).size).toBe(4);});
  it('keeps case-scoped inline tools searchable and reversible',()=>{expect(inlineSource).toContain('Choose a case to review');expect(inlineSource).toContain('Back to case search');expect(inlineSource).toContain('onVisibleStateChange');});
  it('announces workflow stages and gives capability no-match a recovery action',()=>{expect(inlineSource).toContain('aria-live="polite"');expect(inlineSource).toContain('No matching capabilities');expect(inlineSource).toContain('View all capabilities');});
  it('keeps flagship submissions compact and stage replacing',()=>{const appSource=fs.readFileSync(path.join(sharedRoot,'ServiceApp.tsx'),'utf8');expect(inlineSource).toContain('CompactSubmitFlow');expect(inlineSource).toContain('triage-compact-customer-intake');expect(inlineSource).toContain('customer-update-compact-trust-studio');expect(inlineSource).toMatch(/Start another/);expect(inlineSource).toMatch(/setStage\(["']draft["']\)/);expect(appSource).toContain('[data-intent="TriageCustomerIssue"] *');expect(appSource).toContain('[data-intent="ComposeCustomerUpdate"] *');});
  it('reviews incident and recovery work one item at a time',()=>{expect(inlineSource).toContain('FlagshipReview');expect(inlineSource).toContain('incident-candidate-compact-review-queue');expect(inlineSource).toContain('service-recovery-compact-review-queue');expect(inlineSource).toContain('Back to reviews');expect(inlineSource).toContain('Review next item');expect(inlineSource).toContain('Confirm {decision.toLowerCase()}');});
  it('makes every operational capability reachable through paging',()=>{expect(inlineSource).toMatch(/const pageSize\s*=\s*7/);expect(inlineSource).toContain('results.length');expect(inlineSource).toContain('scenarios');expect(inlineSource).toMatch(/Previous/);expect(inlineSource).toMatch(/Next/);expect(inlineSource).not.toContain('results.slice(0,9)');});
  it('does not ship the known inert dashboard actions',()=>{expect(dashboardSource).not.toContain('Review escalation</Button>');expect(dashboardSource).not.toContain('Review swarm package</Button>');expect(dashboardSource).toContain('onOpenRoom');});
  it('keeps Customer 360 searchable and reversible',()=>{expect(dashboardSource).toContain('customer-360-search-directory');expect(dashboardSource).toContain('Back to customer search');expect(dashboardSource).toContain('Search customers');});
  it('keeps leadership answers visually distinct',()=>{expect(inlineSource).toContain('service-performance-demand-river');expect(inlineSource).toContain('regional-service-impact-map');expect(inlineSource).toContain('recurring-service-driver-pareto');expect(inlineSource).toContain('workload-capacity-risk-matrix');expect(inlineSource).toContain('incident-detection-threshold-graph');});
  it('keeps flagship chart answers focused and exact',()=>{const charts=fs.readFileSync(path.join(sharedRoot,'visualizations','ServiceCharts.tsx'),'utf8');expect(charts).toContain('data-chart-answer="demand-resolution"');expect(charts).toContain('data-chart-answer="incident-emergence"');expect(charts).toContain('Last 7 days');expect(charts).toContain('Last 30 days');expect(charts).toContain('Exact cohort values');expect(charts).toContain('<details');});
  it('shows the full-screen label only when width permits',()=>{const appSource=fs.readFileSync(path.join(sharedRoot,'ServiceApp.tsx'),'utf8');expect(appSource).toMatch(/Full screen/);expect(appSource).toMatch(/@media \(max-width: 620px\)["']?:\s*\{\s*display:\s*["']none["']/);});
  it('makes My Queue a guarded judgment workflow',()=>{expect(dashboardSource).toContain('Why this needs judgment');expect(dashboardSource).toContain('Open resolution room');expect(dashboardSource).toContain('Mark resolved');expect(dashboardSource).toContain('Confirm resolved');expect(dashboardSource).toContain('removed from this session queue');});
  it('keeps aggregate leadership answers coordinated and filterable',()=>{const charts=fs.readFileSync(path.join(sharedRoot,'visualizations','ServiceCharts.tsx'),'utf8');expect(inlineSource).toContain('customer-health-portfolio-matrix');expect(charts).toContain('service-operations-filtered-command');expect(charts).toContain('Leadership analysis scope');expect(charts).toContain('Every selection updates');});
  it('publishes a visible-state summary from every inline intent',()=>{expect(inlineSource).toContain('visibleSummary');expect(inlineSource).toContain('visibleStateFor');expect(inlineSource).toMatch(/props\.onVisibleStateChange\?\.\(\s*visibleStateFor/);});
});
